<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mining Shutdown Coordination Portal - Northeastern Ontario</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom animations and styles */
        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .security-indicator {
            background: linear-gradient(45deg, #10b981, #059669);
        }
        
        .hero-gradient {
            background: linear-gradient(135deg, #1e3a8a, #1e40af, #3b82f6);
        }
        
        .card-hover {
            transition: all 0.3s ease;
        }
        
        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body class="bg-gray-50">
    <!-- Main Container -->
    <div id="app">
        <!-- Welcome Page -->
        <div id="welcomePage" class="min-h-screen hero-gradient">
            <!-- Hero Section -->
            <div class="relative">
                <div class="absolute inset-0 bg-black opacity-20"></div>
                <div class="relative z-10 px-4 py-16 text-center text-white">
                    <div class="max-w-4xl mx-auto fade-in">
                        <h1 class="text-5xl font-bold mb-6">
                            Mining Shutdown Coordination Portal
                        </h1>
                        <p class="text-xl mb-8 text-blue-100">
                            Northeastern Ontario's Premier Platform for Mining Operations Coordination
                        </p>
                        <p class="text-lg mb-12 max-w-3xl mx-auto leading-relaxed">
                            Streamline your mining shutdown schedules, avoid resource conflicts, and connect with verified suppliers. 
                            Our secure platform helps mining companies coordinate maintenance windows while enabling suppliers 
                            to discover opportunities across the region.
                        </p>

                        <!-- Value Propositions -->
                        <div class="grid md:grid-cols-3 gap-8 mb-12">
                            <div class="bg-white bg-opacity-10 backdrop-blur-sm rounded-lg p-6 card-hover">
                                <div class="w-12 h-12 mx-auto mb-4 bg-blue-200 rounded-full flex items-center justify-center">
                                    <span class="text-2xl">🛡️</span>
                                </div>
                                <h3 class="text-xl font-semibold mb-2">Secure & Private</h3>
                                <p class="text-blue-100">Bank-grade security with role-based access controls and encrypted data transmission.</p>
                            </div>
                            <div class="bg-white bg-opacity-10 backdrop-blur-sm rounded-lg p-6 card-hover">
                                <div class="w-12 h-12 mx-auto mb-4 bg-blue-200 rounded-full flex items-center justify-center">
                                    <span class="text-2xl">📅</span>
                                </div>
                                <h3 class="text-xl font-semibold mb-2">Smart Coordination</h3>
                                <p class="text-blue-100">AI-powered conflict detection prevents resource scheduling overlaps across the region.</p>
                            </div>
                            <div class="bg-white bg-opacity-10 backdrop-blur-sm rounded-lg p-6 card-hover">
                                <div class="w-12 h-12 mx-auto mb-4 bg-blue-200 rounded-full flex items-center justify-center">
                                    <span class="text-2xl">👥</span>
                                </div>
                                <h3 class="text-xl font-semibold mb-2">Verified Network</h3>
                                <p class="text-blue-100">Connect with pre-qualified suppliers and contractors with proven track records.</p>
                            </div>
                        </div>

                        <!-- Call to Action Buttons -->
                        <div class="flex flex-col sm:flex-row gap-4 justify-center">
                            <button
                                onclick="showIndustrialSignup()"
                                class="bg-white text-blue-900 px-8 py-4 rounded-lg font-semibold text-lg hover:bg-blue-50 transition-colors flex items-center justify-center card-hover"
                            >
                                <span class="mr-2">🏭</span>
                                Industrial Subscriber Access
                            </button>
                            <button
                                onclick="showSupplierSignup()"
                                class="bg-green-500 text-white px-8 py-4 rounded-lg font-semibold text-lg hover:bg-green-600 transition-colors flex items-center justify-center card-hover"
                            >
                                <span class="mr-2">🚛</span>
                                Supplier & Vendor Portal
                            </button>
                        </div>

                        <div class="mt-8">
                            <button
                                onclick="showLogin()"
                                class="text-blue-200 hover:text-white underline"
                            >
                                Already have an account? Sign in here
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Features Section -->
            <div class="bg-white py-16">
                <div class="max-w-6xl mx-auto px-4">
                    <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">
                        Why Choose Our Platform?
                    </h2>
                    
                    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
                        <div class="text-center">
                            <div class="bg-blue-100 rounded-full p-4 w-16 h-16 mx-auto mb-4 flex items-center justify-center">
                                <span class="text-2xl">⚠️</span>
                            </div>
                            <h3 class="font-semibold text-lg mb-2">Conflict Prevention</h3>
                            <p class="text-gray-600">Automatically detect and prevent resource scheduling conflicts</p>
                        </div>
                        
                        <div class="text-center">
                            <div class="bg-green-100 rounded-full p-4 w-16 h-16 mx-auto mb-4 flex items-center justify-center">
                                <span class="text-2xl">📍</span>
                            </div>
                            <h3 class="font-semibold text-lg mb-2">Regional Coverage</h3>
                            <p class="text-gray-600">Complete coverage of Northeastern Ontario mining operations</p>
                        </div>
                        
                        <div class="text-center">
                            <div class="bg-purple-100 rounded-full p-4 w-16 h-16 mx-auto mb-4 flex items-center justify-center">
                                <span class="text-2xl">🛡️</span>
                            </div>
                            <h3 class="font-semibold text-lg mb-2">Compliance Ready</h3>
                            <p class="text-gray-600">Built-in compliance tracking and audit trails</p>
                        </div>
                        
                        <div class="text-center">
                            <div class="bg-orange-100 rounded-full p-4 w-16 h-16 mx-auto mb-4 flex items-center justify-center">
                                <span class="text-2xl">🏆</span>
                            </div>
                            <h3 class="font-semibold text-lg mb-2">Verified Suppliers</h3>
                            <p class="text-gray-600">Pre-qualified suppliers with rating and review system</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Statistics Section -->
            <div class="bg-gray-100 py-12">
                <div class="max-w-4xl mx-auto px-4 text-center">
                    <div class="grid md:grid-cols-4 gap-8">
                        <div>
                            <div class="text-3xl font-bold text-blue-600 mb-2">25+</div>
                            <div class="text-gray-600">Mining Operations</div>
                        </div>
                        <div>
                            <div class="text-3xl font-bold text-green-600 mb-2">150+</div>
                            <div class="text-gray-600">Verified Suppliers</div>
                        </div>
                        <div>
                            <div class="text-3xl font-bold text-purple-600 mb-2">$50M+</div>
                            <div class="text-gray-600">Coordinated Projects</div>
                        </div>
                        <div>
                            <div class="text-3xl font-bold text-orange-600 mb-2">99.8%</div>
                            <div class="text-gray-600">Uptime Guarantee</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Industrial Signup Page -->
        <div id="industrialSignupPage" class="min-h-screen bg-gray-50 py-12" style="display: none;">
            <div class="max-w-md mx-auto bg-white rounded-lg shadow-lg p-8">
                <!-- Security Status -->
                <div class="security-indicator rounded-lg p-3 mb-4 text-white">
                    <div class="flex items-center">
                        <span class="mr-2">🛡️</span>
                        <div class="flex-1">
                            <p class="text-sm font-medium">Security Status: Protected</p>
                            <div class="text-xs mt-1 space-x-4">
                                <span>🔒 SSL/TLS Encrypted</span>
                                <span>🛡️ OWASP Compliant</span>
                                <span>🔐 MFA Required</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="text-center mb-8">
                    <div class="w-12 h-12 mx-auto mb-4 bg-blue-600 rounded-full flex items-center justify-center">
                        <span class="text-white text-xl">🏭</span>
                    </div>
                    <h2 class="text-2xl font-bold">Industrial Subscriber Registration</h2>
                    <p class="text-gray-600 mt-2">Secure access to the coordination network</p>
                </div>

                <form onsubmit="submitIndustrialForm(event)" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium mb-1">Company Name *</label>
                        <input type="text" id="companyName" class="w-full p-3 border rounded-lg" placeholder="Your mining company" required>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium mb-1">Primary Contact Name *</label>
                        <input type="text" id="contactName" class="w-full p-3 border rounded-lg" placeholder="John Smith" required>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium mb-1">Business Email *</label>
                        <input type="email" id="email" class="w-full p-3 border rounded-lg" placeholder="contact@company.com" required>
                    </div>

                    <div>
                        <label class="block text-sm font-medium mb-1">Secure Password *</label>
                        <input type="password" id="password" class="w-full p-3 border rounded-lg" placeholder="Create strong password" required>
                        <div id="passwordStrength" class="mt-2 text-sm text-gray-600">
                            Password must be at least 12 characters with uppercase, lowercase, numbers, and special characters.
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium mb-1">Mining Operations Location(s)</label>
                        <select multiple class="w-full p-3 border rounded-lg h-24">
                            <option>Timmins</option>
                            <option>Sudbury</option>
                            <option>Kirkland Lake</option>
                            <option>Val-d'Or</option>
                            <option>Rouyn-Noranda</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium mb-1">Phone Number</label>
                        <input type="tel" class="w-full p-3 border rounded-lg" placeholder="(705) 555-0123">
                    </div>

                    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                        <h4 class="font-semibold text-blue-800 mb-2 flex items-center">
                            <span class="mr-2">🔒</span>
                            Security & Compliance Features:
                        </h4>
                        <ul class="text-sm text-blue-700 space-y-1">
                            <li>• End-to-end encryption for all data</li>
                            <li>• Multi-factor authentication required</li>
                            <li>• OWASP Top 10 security compliance</li>
                            <li>• Regular security audits and monitoring</li>
                            <li>• Role-based access controls</li>
                        </ul>
                    </div>

                    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-3">
                        <p class="text-xs text-yellow-800">
                            <strong>Privacy Notice:</strong> Your data is encrypted and never shared with third parties. 
                            We comply with PIPEDA and other applicable privacy regulations.
                        </p>
                    </div>

                    <button
                        type="submit"
                        class="w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors"
                    >
                        Register & Request Secure Access
                    </button>
                </form>

                <div class="text-center mt-6">
                    <button onclick="showWelcome()" class="text-blue-600 hover:underline">
                        Back to Welcome
                    </button>
                </div>
            </div>
        </div>

        <!-- Supplier Signup Page -->
        <div id="supplierSignupPage" class="min-h-screen bg-gray-50 py-12" style="display: none;">
            <div class="max-w-md mx-auto bg-white rounded-lg shadow-lg p-8">
                <div class="security-indicator rounded-lg p-3 mb-4 text-white">
                    <div class="flex items-center">
                        <span class="mr-2">🛡️</span>
                        <div class="flex-1">
                            <p class="text-sm font-medium">Security Status: Protected</p>
                            <div class="text-xs mt-1 space-x-4">
                                <span>🔒 SSL/TLS Encrypted</span>
                                <span>🛡️ OWASP Compliant</span>
                                <span>🔐 MFA Required</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="text-center mb-8">
                    <div class="w-12 h-12 mx-auto mb-4 bg-green-600 rounded-full flex items-center justify-center">
                        <span class="text-white text-xl">🚛</span>
                    </div>
                    <h2 class="text-2xl font-bold">Supplier & Vendor Registration</h2>
                    <p class="text-gray-600 mt-2">Connect with mining operations</p>
                </div>

                <form onsubmit="submitSupplierForm(event)" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium mb-1">Company Name *</label>
                        <input type="text" class="w-full p-3 border rounded-lg" placeholder="Your service company" required>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium mb-1">Service Categories</label>
                        <select multiple class="w-full p-3 border rounded-lg h-24">
                            <option>Heavy Equipment</option>
                            <option>Electrical Services</option>
                            <option>Millwright Services</option>
                            <option>Specialized Contractors</option>
                            <option>Safety Equipment</option>
                        </select>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium mb-1">Contact Email *</label>
                        <input type="email" class="w-full p-3 border rounded-lg" placeholder="info@supplier.com" required>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium mb-1">Service Area</label>
                        <select multiple class="w-full p-3 border rounded-lg h-20">
                            <option>Timmins Region</option>
                            <option>Sudbury Region</option>
                            <option>Kirkland Lake Region</option>
                            <option>Regional (All Areas)</option>
                        </select>
                    </div>

                    <div class="bg-green-50 border border-green-200 rounded-lg p-4">
                        <h4 class="font-semibold text-green-800 mb-2">Supplier Benefits Include:</h4>
                        <ul class="text-sm text-green-700 space-y-1">
                            <li>• Discovery by mining companies</li>
                            <li>• Shutdown schedule visibility</li>
                            <li>• Direct procurement portal links</li>
                            <li>• Performance ratings system</li>
                            <li>• Mobile notifications</li>
                        </ul>
                    </div>

                    <button
                        type="submit"
                        class="w-full bg-green-600 text-white py-3 rounded-lg font-semibold hover:bg-green-700 transition-colors"
                    >
                        Register & Request Verification
                    </button>
                </form>

                <div class="text-center mt-6">
                    <button onclick="showWelcome()" class="text-green-600 hover:underline">
                        Back to Welcome
                    </button>
                </div>
            </div>
        </div>

        <!-- Login Page -->
        <div id="loginPage" class="min-h-screen bg-gray-50 py-12" style="display: none;">
            <div class="max-w-md mx-auto bg-white rounded-lg shadow-lg p-8">
                <div class="security-indicator rounded-lg p-3 mb-4 text-white">
                    <div class="flex items-center">
                        <span class="mr-2">🛡️</span>
                        <div class="flex-1">
                            <p class="text-sm font-medium">Security Status: Protected</p>
                            <div class="text-xs mt-1 space-x-4">
                                <span>🔒 SSL/TLS Encrypted</span>
                                <span>🛡️ OWASP Compliant</span>
                                <span>🔐 MFA Required</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="text-center mb-8">
                    <div class="w-12 h-12 mx-auto mb-4 bg-blue-600 rounded-full flex items-center justify-center">
                        <span class="text-white text-xl">🛡️</span>
                    </div>
                    <h2 class="text-2xl font-bold">Secure Sign In</h2>
                    <p class="text-gray-600 mt-2">Access your mining coordination portal</p>
                </div>

                <form onsubmit="submitLogin(event)" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium mb-1">Email *</label>
                        <input type="email" class="w-full p-3 border rounded-lg" placeholder="your@company.com" required>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium mb-1">Password *</label>
                        <input type="password" class="w-full p-3 border rounded-lg" placeholder="••••••••" required>
                    </div>

                    <div class="flex items-center justify-between">
                        <label class="flex items-center">
                            <input type="checkbox" class="mr-2">
                            <span class="text-sm">Remember me (this device)</span>
                        </label>
                        <button type="button" class="text-sm text-blue-600 hover:underline">
                            Forgot password?
                        </button>
                    </div>

                    <div class="bg-gray-50 rounded-lg p-3">
                        <div class="flex items-center text-sm text-gray-600">
                            <span class="mr-2">🔑</span>
                            <span>Multi-factor authentication will be required after login</span>
                        </div>
                    </div>

                    <button
                        type="submit"
                        class="w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors"
                    >
                        Sign In Securely
                    </button>
                </form>

                <div class="mt-6 text-center">
                    <div class="text-xs text-gray-500 mb-4">
                        Your connection is secured with 256-bit SSL encryption
                    </div>
                    <button onclick="showWelcome()" class="text-blue-600 hover:underline">
                        Back to Welcome
                    </button>
                </div>
            </div>
        </div>

        <!-- Success Page -->
        <div id="successPage" class="min-h-screen bg-gray-50 flex items-center justify-center" style="display: none;">
            <div class="bg-white rounded-lg shadow-lg p-8 max-w-md text-center">
                <div class="w-16 h-16 mx-auto mb-4 bg-green-100 rounded-full flex items-center justify-center">
                    <span class="text-2xl">✅</span>
                </div>
                <h2 class="text-2xl font-bold mb-4">Registration Successful!</h2>
                <p class="text-gray-600 mb-6">
                    Thank you for registering with the Mining Shutdown Coordination Portal. 
                    Your application has been received and is under review.
                </p>
                <p class="text-sm text-gray-500 mb-6">
                    You will receive an email confirmation within 24 hours with your account activation details.
                </p>
                <button onclick="showWelcome()" class="bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors">
                    Return to Welcome
                </button>
            </div>
        </div>
    </div>

    <!-- JavaScript -->
    <script>
        // Page navigation functions
        function showWelcome() {
            hideAllPages();
            document.getElementById('welcomePage').style.display = 'block';
        }

        function showIndustrialSignup() {
            hideAllPages();
            document.getElementById('industrialSignupPage').style.display = 'block';
        }

        function showSupplierSignup() {
            hideAllPages();
            document.getElementById('supplierSignupPage').style.display = 'block';
        }

        function showLogin() {
            hideAllPages();
            document.getElementById('loginPage').style.display = 'block';
        }

        function showSuccess() {
            hideAllPages();
            document.getElementById('successPage').style.display = 'block';
        }

        function hideAllPages() {
            const pages = ['welcomePage', 'industrialSignupPage', 'supplierSignupPage', 'loginPage', 'successPage'];
            pages.forEach(pageId => {
                document.getElementById(pageId).style.display = 'none';
            });
        }

        // Form submission handlers
        function submitIndustrialForm(event) {
            event.preventDefault();
            
            // Basic validation
            const companyName = document.getElementById('companyName').value;
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            
            if (!companyName || !email || !password) {
                alert('Please fill in all required fields.');
                return;
            }
            
            if (password.length < 12) {
                alert('Password must be at least 12 characters long.');
                return;
            }
            
            // Simulate successful registration
            showSuccess();
        }

        function submitSupplierForm(event) {
            event.preventDefault();
            showSuccess();
        }

        function submitLogin(event) {
            event.preventDefault();
            alert('Demo Mode: Login functionality will be available in the full version. Thank you for testing the portal!');
        }

        // Password strength indicator
        document.addEventListener('DOMContentLoaded', function() {
            const passwordInput = document.getElementById('password');
            if (passwordInput) {
                passwordInput.addEventListener('input', function() {
                    const password = this.value;
                    const strengthDiv = document.getElementById('passwordStrength');
                    
                    if (password.length === 0) {
                        strengthDiv.textContent = 'Password must be at least 12 characters with uppercase, lowercase, numbers, and special characters.';
                        strengthDiv.className = 'mt-2 text-sm text-gray-600';
                    } else if (password.length < 12) {
                        strengthDiv.textContent = 'Password too short. Need ' + (12 - password.length) + ' more characters.';
                        strengthDiv.className = 'mt-2 text-sm text-red-600';
                    } else {
                        const hasUpper = /[A-Z]/.test(password);
                        const hasLower = /[a-z]/.test(password);
                        const hasNumber = /\d/.test(password);
                        const hasSpecial = /[!@#$%^&*()_+\-=\[\]{};':"\\|,.<>\?]/.test(password);
                        
                        if (hasUpper && hasLower && hasNumber && hasSpecial) {
                            strengthDiv.textContent = 'Strong password ✓';
                            strengthDiv.className = 'mt-2 text-sm text-green-600';
                        } else {
                            strengthDiv.textContent = 'Need uppercase, lowercase, number, and special character.';
                            strengthDiv.className = 'mt-2 text-sm text-yellow-600';
                        }
                    }
                });
            }
        });
    </script>
</body>
</html>
